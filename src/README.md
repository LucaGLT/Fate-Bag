Questo repository contiene il codice sorgente dell'applicazione Token/Card.
La cartella `src` è organizzata in moduli: `core`, `infrastructure`, `gui_pyqt` e `tests`.
Ogni sottocartella contiene un `README.md` descrittivo per permettere il commit.
