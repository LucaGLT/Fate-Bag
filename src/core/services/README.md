Contiene i servizi applicativi che orchestrano engine, repository ed eventi.
Esempi:
- `TokenService`
- `SessionService`
- `DrawService`

I servizi non devono contenere regole di UI, solo orchestrazione e coordinamento.
